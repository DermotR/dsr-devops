{
  "name": "{{PROJECT_NAME}}-client",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "{{CLIENT_START_COMMAND}}",
    "build": "{{CLIENT_BUILD_COMMAND}}",
    "test": "{{CLIENT_TEST_COMMAND}}",
    "lint": "eslint src --ext js,jsx,ts,tsx"
  },
  "dependencies": {
    "axios": "^1.4.0"
  },
  "devDependencies": {
    "eslint": "^8.42.0",
    "eslint-plugin-react": "^7.32.2"
  }
}