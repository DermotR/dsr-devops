{
  "name": "{{PROJECT_NAME}}-server",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "start": "{{SERVER_START_COMMAND}}",
    "build": "{{SERVER_BUILD_COMMAND}}",
    "test": "{{SERVER_TEST_COMMAND}}",
    "lint": "eslint src --ext js,ts"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^8.42.0",
    "nodemon": "^2.0.22"
  }
}